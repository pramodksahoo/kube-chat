{{- if .Values.nlpService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubechat.fullname" . }}-nlp-service
  labels:
    {{- include "kubechat.labels" . | nindent 4 }}
    app.kubernetes.io/component: nlp-service
  {{- with .Values.nlpService.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.nlpService.service.type }}
  ports:
  - port: {{ .Values.nlpService.service.port }}
    targetPort: {{ .Values.nlpService.service.targetPort | default "http" }}
    protocol: TCP
    name: http
  selector:
    {{- include "kubechat.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: nlp-service
{{- end }}